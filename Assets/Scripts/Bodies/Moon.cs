using UnityEngine;

/*
 * a class to represent a moon in the universe.
 */
public class Moon : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
